<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Rubyonrails" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Rubyonrails" %>">
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= yield(:head) %>
  </head>
  <body class="<%= controller_name %> <%= action_name %>">
  <div class="container">
    <div class="row">
      <div class="span12">
        <div class="navbar">
          <div class="navbar-inner">
            <a class="brand" href="/">Get Blogging - Rails</a>
            <ul class="nav">
              <li><a href="/">Home</a></li>
              <% if user_signed_in? %>
                  <li><a href="/posts/">Posts</a></li>
                  <li><a href="/categories/">Categories</a></li>
                  <li><%= link_to 'Logout', destroy_user_session_path, :method=>'delete' %></li>
              <% else %>
                  <li><%= link_to 'Login', new_user_session_path %></li>
              <% end %>
            </ul>
          </div>
        </div>
      </div>
      <hr />
    </div>
    <div class="row">
      <div class="span12 posts">
        <%= yield %>
      </div>
    </div>
  </div>
  <script>
      $('.post-body').jTruncate("p");
  </script>
  </body>
</html>
